# TSM-SeniorOasisPanel PDF-VNC Integration Requirements
# Advanced PDF generation with VNC remote control capabilities

# Core Dependencies
tkinter>=8.6.0
PIL>=8.0.0
numpy>=1.19.0
scipy>=1.5.0

# VNC Dependencies
websockify>=0.9.0
websockets>=8.0.0
tornado>=6.0.0

# PDF Dependencies
reportlab>=3.5.0
PyPDF2>=2.0.0
pypdf>=3.0.0

# Network Dependencies
requests>=2.25.0
urllib3>=1.26.0
socket>=1.0.0

# System Dependencies
psutil>=5.8.0
pywin32>=227; sys_platform == "win32"

# Image Processing
opencv-python>=4.5.0
matplotlib>=3.3.0

# Compression and Encoding
zlib-ng>=0.2.0
base64>=1.0.0

# Threading and Concurrency
threading>=1.0.0
multiprocessing>=1.0.0

# File Operations
pathlib>=1.0.0
shutil>=1.0.0
tempfile>=1.0.0

# JSON and Data Handling
json>=2.0.0
pickle>=1.0.0

# Date and Time
datetime>=1.0.0
time>=1.0.0

# Subprocess and System
subprocess>=1.0.0
os>=1.0.0
sys>=1.0.0

# GUI Dependencies (tkinter is usually included with Python)
# ttk (part of tkinter)
# scrolledtext (part of tkinter)
# filedialog (part of tkinter)
# messagebox (part of tkinter)

# Optional Dependencies for Enhanced Features
# LaTeX support (system dependency - pdflatex)
# VNC server (system dependency - TigerVNC, TightVNC, etc.)
# Web browser (system dependency - for web interfaces)

# Development Dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.10.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.800

# Documentation Dependencies (optional)
sphinx>=3.4.0
sphinx-rtd-theme>=0.5.0
